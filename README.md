# Native app

prerequisites: xCode and simulator

1. clone the repo
2. then install all the dependancies:
   `yarn install`
   `cd ios/`
   `pod install`
   `cd ..`

3.

run the app on ios :
`yarn s`
